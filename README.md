# YouTube Downloader

## Описание
Программа скачивает аудио и видео дорожки с YouTube в любом выбранном качестве, используя модуль `yt_dlp`. Скрипт позволяет вручную выбирать ID форматов и объединяет их в один `.mkv` файл с помощью `ffmpeg`.

## Требования
1. **Python 3**: Должен быть установлен и добавлен в PATH.

## Использование

### 1. Создайте виртуальное окружение (если хотите). Установите необходимые библиотеки. Для этого выполните в терминале:
```bash
python -m pip install -r requirements.txt
```

### 2. Запустите файл `yt_downloader.py`. 
   Скрипт автоматически обновит необходимые библиотеки (`yt-dlp`), при необходимости скачает `ffmpeg` и `deno` и запустит основной скрипт.

### 2. Следуйте инструкциям программы:
   **Enter video URL**: Вставьте ссылку на видео.  
   **Enter Video ID / Audio ID**: Введите выбранные номера из списков.  

### 3. Готовый файл появится в папке `Downloads/` в корне проекта.

## Известные проблемы и нюансы

Актуально c версией yt_dlp 2026.08.19. Периодически при скачивании некоторых форматов видео и аудио (обычно самык крутых по качеству) youtube запрашивает куки браузера, скрипт падает с ошибкой. Работаю над исправлением, не требующим куки